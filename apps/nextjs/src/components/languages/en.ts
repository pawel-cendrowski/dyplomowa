import { LanguageDefaultType } from "./types/types";

export const en :LanguageDefaultType = {
    common:{
        read_more:"Read more",
        loading:"Loading",
        error_404:"Page not found"
    }
};